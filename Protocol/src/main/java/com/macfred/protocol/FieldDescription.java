package com.macfred.protocol;

public class FieldDescription {
    public int start;
    public int length;

    public FieldDescription(int start, int length) {
        this.start = start;
        this.length = length;
    }
}
