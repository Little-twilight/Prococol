apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    api 'com.github.Little-twilight:JavaUtils:master-SNAPSHOT'
}

sourceCompatibility = "8"
targetCompatibility = "8"
